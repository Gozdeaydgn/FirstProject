// 1. Write an Apex code that store two numbers in Integer variables
//    - Print out the bigger number

Integer num1 = 100, num2 = 200;

if (num1 > num2) {
  System.debug('Num1 is the biggest number');
} else if (num2 > num1) {
  System.debug('Num2 is the biggest number');
}
