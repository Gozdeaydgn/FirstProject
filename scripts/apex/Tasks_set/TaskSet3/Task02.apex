/**
2. Create an Integer variable with name upperLimit to control where to stop and initialize it's value to 15. 
Create a for loop to count from 1 to the upperLimit , try different upperLimit and run again
 */

Integer upperLimit = 15;


for(Integer count = 1 ; count<= upperLimit ; count++){
    System.debug(count); 
}
